import styled from '@emotion/styled';

export const MainLayoutWrapper = styled.div`
  margin: 0 auto;
  padding: 1.3rem;
  width: min(520px, 100%);
  background-color: #edeef0;
  min-height: 100vh;
`;
